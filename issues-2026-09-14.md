# Weekly Democracy Brief — Issues Log, 2026-09-14

## Data source failures this run

### Metaculus API (DTI forecasts)
- **Problem**: `GET /api2/questions/?project=32829` returned count=0. Tournament page returns 403. v3 API returns 404.
- **Impact**: No Metaculus community forecast probabilities were included in the DTI table. Forecaster counts and links only. Has persisted across editions.
- **Suggested fix**: Check whether the DTI project ID (32829) is still current; try fetching via `tournament_slug` parameter instead of project ID. Alternatively, fetch individual question URLs listed on the Metaculus DTI tournament page.

### Sabato Crystal Ball RSS feed
- **Problem**: HTTP 403 from Cloudflare on RSS fetch.
- **Impact**: No Sabato rating changes sourced this edition.
- **Suggested fix**: Try Wayback Machine or look for a non-RSS alternative (e.g., the Crystal Ball homepage HTML). Consider adding Split Ticket as a backup rating source.

### 270toWin
- **Problem**: JavaScript-rendered; ratings not extractable from raw HTML response.
- **Impact**: No 270toWin ratings sourced.
- **Suggested fix**: Consider using Playwright/Puppeteer headless browser, or scraping Cook/Inside Elections directly.

### Press Freedom Tracker API
- **Problem**: `/api/incidents/?format=json` returned HTTP 404.
- **Impact**: Press Freedom subsection omitted (no incidents in window Sep 9–14; most recent HTML incidents are Aug 29).
- **Suggested fix**: Check current API endpoint documentation. Try `/api/v1/incidents/` or other path variants. Also investigate if data entry is lagging (incidents may exist but not yet entered).

### Kalshi API
- **Problem**: Public endpoint returned empty array; authenticated endpoint returns 401.
- **Impact**: No Kalshi-specific recession or election probabilities available. EBO aggregates Kalshi and is used instead.
- **Suggested fix**: Look into whether Kalshi has an unofficial public endpoint or rate-limited unauthenticated access.

### SCOTUSblog article URL
- **Problem**: The SCOTUSblog article confirming the Missouri stay ("Supreme Court grants request to prevent Missouri from using congressional map expected to aid Republicans," Sep 10) was confirmed on the homepage but the URL slug was unresolvable (all candidate slugs returned 404). SCOTUSblog appears to use Sanity CMS with non-predictable URL patterns.
- **Impact**: The article link in the What Changed section was replaced with scotusblog.com homepage. The claim itself is correctly verified.
- **Suggested fix**: Try fetching SCOTUSblog's Sanity API directly to get the article slug, or monitor for the article URL via Google News search.

### Silver Bulletin FLIPR (STEP 4.8 note)
- **Problem**: Silver Bulletin approval tracker shows -19.5 (matching prior edition) — same value two editions in a row. Could indicate the tracker updates less frequently than weekly.
- **Impact**: Low. KPI reported as "unchanged."

## Claims removed during STEP 4.8 verification

1. **SCOTUSblog article URL** (broken_link): Replaced with homepage URL. See `pending_review` in audit.json.

## Data quality flags

- **Recession KPI direction**: 7.5% → 8.5% (increase in recession risk, bad news). The "dn" class is applied in the KPI figure, which displays the change as unfavorable. The direction is semantically correct.
- **RFK Jr. GOP 2028 nomination**: First appearance in tracking (no prior value, Δ shows —). Explanatory footnote added re: overround in binary nomination markets.
- **Michigan Democratic candidate**: Market now shows Abdul El-Sayed; prior editions tracked McMorrow. History key `senate_michigan` updated with El-Sayed values from this edition forward.

## iMessage delivery status
- Attempted via `~/.config/weekly-brief/send-imessage.sh` — see below.
