7/17/26

# Weekly Brief — Run Issues (2026-07-17)

This edition was generated **manually in an interactive Claude Code (Opus) session**, not by the automated Sunday job. Two expired credentials are the root cause of the ~5-week publishing gap (last edition June 15) and both need your action to restore automation.

## Blockers on the automated pipeline (need Alex action)

1. **Claude CLI OAuth token expired.** The launchd job `com.lintz.weekly-brief` has been firing but failing every run since ~June 15 with `401 OAuth access token has expired. Re-authenticate to continue.` The wrapper retries 3x and exits; nothing publishes. **Fix:** run `claude` interactively once and complete the login flow so the CLI token refreshes. Until then, the Sunday automation will keep silently failing.

2. **GitHub PAT expired.** Both `~/.config/weekly-brief/secrets.env` (`$GH_PAT`) and the status-ping token return `401 Bad credentials`. This run published via the `gh` CLI keyring auth (account `allintz`) instead. The run-status checkpoint pings have also been dead since ~June 22 for the same reason. **Fix:** regenerate the fine-grained PAT (Contents: read/write, this repo only) and update `~/.config/weekly-brief/secrets.env` and `~/.config/weekly-intel-brief-a9x/pat`.

## Data gaps / judgment calls this run

3. **Metaculus community distributions unavailable.** The API returns forecaster counts (Q5121: 1,874; Q4123: 243; Q37321: 250) but no aggregation/`centers` data even with `?with_cp=true`. Community distributions omitted and linked out, as in recent editions. Suggested fix: try the dedicated aggregation endpoint or an unauthenticated request; this has recurred for months.

4. **Ohio Senate market vs. polling divergence.** Thin Polymarket ($104K) priced Husted (R) 52% / Brown (D) 49% — within the bid/ask spread — while a Fox poll had Brown +8 and Kalshi ~56% Brown. Presented as a genuine toss-up with the divergence noted rather than "Husted leads."

5. **RFK Jr. GOP-nominee sub-market excluded.** Showed a nominal 49% but with `volumeNum=None` and no last trade (stale placeholder); absent from the overall 2028-winner market. Excluded; Vance (41.5%) shown as leader.

6. **Bright Line Watch exact score not re-verifiable.** No new wave since the May 2026 report; the live homepage frames the latest report qualitatively and doesn't restate a single number. Kept ~57/100 as the last published figure with that caveat.

7. **Two source links 403 to automated fetch (bot-block, not dead).** Georgia Recorder (Fulton subpoena quashed) and Axios (NY data-center EO) returned 403 to curl but are real articles that load in a browser. The Axios link was swapped to the NY Governor's primary press release; the Georgia Recorder link was retained as the best primary source.

## Notes
- Gap context: 5 weeks since the June 15 edition, so week-over-week deltas are measured against June 15 and labeled "since Jun 15" throughout.
- Three SCOTUS markets resolved this period (birthright struck down; Humphrey's Executor overruled; mail-ballot grace period upheld) and the Anthropic Fable 5 export controls were lifted June 30 — all verified against primary sources (SCOTUSblog, Votebeat, CNBC).
- Claim audit: 38 extracted, 30 audited, 25 supported, 4 softened, 1 excluded, 2 unable to verify, 0 hard-fail unresolved.
