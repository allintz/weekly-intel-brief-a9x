You are applying Alex's editorial feedback to the published 2026-04-20 weekly intelligence brief. Your job: apply every edit below, commit both HTML files, and push to main.

## Files to edit

- `/Users/alexlintz/code/prediction-tracker/archive/2026-04-20.html` (canonical)
- `/Users/alexlintz/code/prediction-tracker/index.html` (must be identical copy after edits)

## Process

1. Read the HTML fully, identify each affected section by the headers/classes shown below
2. Apply the edits (deletions, replacements, additions) to `archive/2026-04-20.html`
3. Copy archive/2026-04-20.html to index.html (they must match verbatim)
4. `cd ~/code/prediction-tracker && git add archive/2026-04-20.html index.html && git commit -m "Apply editorial feedback to 2026-04-20 edition" && git push origin main`

## Edits to apply

### REMOVALS (delete entire block)

1. **"Movers & Approaching Resolution" section** — the standalone table of movers at the top. Instead, ensure the main democracy markets table (Section 1) and other markets tables have a `Δ Week` column with percent-point change displayed inline. Highlight moved rows with a subtle background color (e.g., `background: var(--amber-dim)` for rows where |Δ| ≥ 5pp).
2. **"🎯 This Week's Bottom Line" / Executive Summary callout** — remove the entire purple callout box that appears below the Watching cards.
3. **"Democracy Threat Index — Metaculus Project 32829" card** — entire card. The Metaculus API returned null aggregations this run; don't show a placeholder.
4. **BLW Historical Trajectory TABLE** — keep the small BLW card showing current score (~44 for Spring 2026) + the "Methodology & trajectory deep dive ↓" link. REMOVE the full historical wave table (Summer 2018, Winter 2020–21, Fall 2022, Winter 2024–25, Spring 2026 rows) from Section 1. The full table stays in the appendix only.
5. **DOJ terrorism-adjacent paragraph** — any line saying "DOJ terrorism-adjacent frameworks remain the primary instrument... [no new enforcement actions this week]" should be deleted. Rule: if a bullet says "no new X this week" or "pattern from prior reporting", delete the bullet entirely.
6. **Forecaster Commentary fetch-error block** — any text that says "Cook Political Report (403 this run)", "Sabato's Crystal Ball... not accessible", "Silver Bulletin was not accessible", or similar fetch-failure verbiage. Do NOT show these to readers. Delete the entire Forecaster Commentary subsection if it has no confirmed rating changes this week.
7. **Press Freedom Tracker**: only keep incidents from the past 7 days (today is April 20). April 11 = 9 days ago → remove. April 8 = 12 days ago → remove. If no incidents in past 7 days, delete the incidents list entirely but keep the RSF (57th) and V-Dem (0.57) annual reference anchors as one short line. Delete the line "No new incidents recorded in the April 13–20 window per the tracker database."
8. **Any "Auto-updates every Monday 3am ET" badge** — change to "Auto-updates every Sunday 7pm ET" (schedule changed).

### RESTRUCTURE

9. **Watching This Week cards** — reduce vertical space:
   - `padding: 14px 18px` (was larger)
   - `font-size: 13px` for the summary text
   - `h4` at 14px
   - Remove or shorten tooltips that add bulk
   - Use `margin-bottom: 8px` between cards

10. **Democratic / Republican Nominee 2028** — currently shows mixed list with J.D. Vance incorrectly in the Dem list.
    - Split into TWO side-by-side cards: "Democratic Nominee 2028" and "Republican Nominee 2028"
    - Each sorted DESCENDING by probability
    - Each candidate row shows: name, current probability, WoW Δ (absolute pp change, not relative %)
    - Dem list (from Polymarket `democratic-presidential-nominee-2028`): current order should be Newsom 19.3% → AOC 5.9% → Shapiro 2.9% → Buttigieg 2.6% → next highest (query Polymarket for top 5). REMOVE J.D. Vance entirely — he's Republican.
    - GOP list (from Polymarket `republican-presidential-nominee-2028` + Metaculus Q37321): top 5 descending.
    - WoW deltas: compute against last week's values if available in the prior edition (`archive/2026-04-15.html`). If a candidate isn't in the prior edition, show `new` instead of a Δ.

11. **S&P 500 card** — add WoW % change below the absolute value:
    - Look up last week's S&P value from `archive/2026-04-15.html` (search for "S&P 500")
    - Render like: `~7,080` (big number) then below: `WoW: +X.X%` with green/red coloring based on direction
    - Same treatment for CPI / inflation card and unemployment card: show WoW change alongside absolute

12. **Lump all AI-related prediction markets together** — currently split between Section 4 "AI Pause & Moratorium markets" and possibly Section 3. Consolidate into ONE table in Section 4 titled "AI-related prediction markets" including:
    - AI data center moratorium before 2027 (Polymarket)
    - US AI safety bill before 2027 (Polymarket)
    - AI moratorium — Metaculus Q38766
    - OpenAI announces AGI before 2027 (Polymarket)
    - Anthropic CEO arrested
    - Any other AI-related market currently scattered in the page
    Columns: Market / Prob / Δ Week / Volume / Activity / Source

13. **AGI timeline section (Section 3)** — read `/Users/alexlintz/code/prediction-tracker/archive/2026-04-15.html` and compare to current Section 3 content. If the expert forecast visualization (Kokotajlo, Greenblatt, Lifland, Cotra, etc.) is unchanged from last week, compress it dramatically: remove the full chart and instead show a single line: "Expert AGI forecasts — see [prior edition](/archive/2026-04-15) for full breakdown. This week's changes: [list only what changed]." Keep the Goodheart Labs combined forecast card (updates weekly). Keep Community Forecasts (Metaculus 5121 etc.) but only if those have moved >5% since last week. Everything stays linked to prior edition.

### ADDITIONS

14. **Indiana state senate primaries** — Indiana holds state primary elections May 5, 2026 (first Tuesday of May). Add to:
    - Key Dates & Deadlines ("This Month" column): `May 5 — Indiana state primaries (state Senate + House)`
    - Watching This Week cards: add a card labeled "MAY 5 — 2 WEEKS" / `near` (amber) class, title "Indiana State Primaries", summary "State senate and house primary elections. Alex's personal stake — watch for competitive district outcomes and turnout indicators."

15. **CAISI rename context in Section 4** — The US government's AI Safety Institute (AISI) at NIST was renamed to the **Center for AI Standards and Innovation (CAISI)**. Add a bullet or small callout in Section 4 Policy Tracker:
    - Title: "CAISI rename"
    - Context: "NIST's AI Safety Institute was renamed the Center for AI Standards and Innovation; now releasing sector-specific RMF profiles. The rename signals a deliberate shift from 'safety' to 'standards and innovation' framing. If CAISI focuses on adoption barriers rather than risk, the US government's primary AI evaluation body is no longer oriented toward catching risks before deployment."
    - WebFetch NIST's page or news coverage to confirm the rename date. If you can't confirm a specific date, write "announced early 2026 — date not confirmed" rather than fabricating.

### CONTENT RULES (apply throughout)

- If any section has no content remaining after these edits, delete the section header too
- Never show fetch-error messages to readers (no "Cook returned 403", no "Metaculus null", no "can't access")
- Past-7-days filter on all dated news items: today is April 20; anything older than April 13 should be removed unless it's genuinely new reporting from this week
- Nominee probabilities: sort descending, show WoW Δ, separate Dem from GOP
- Don't fabricate dates or sources

### DO NOT

- Don't re-fetch live market data (the existing market figures stay as-is for this edit pass)
- Don't add new intel bullets
- Don't change CSS tokens (`:root` vars) or other structural styles
- Don't add "This edit was produced by..." notes anywhere
- Don't commit to run-status or publish a new edition — this is just applying edits to the already-published 2026-04-20 edition

### Verify before commit

- Run `python3 -c "print(open('/Users/alexlintz/code/prediction-tracker/archive/2026-04-20.html').read() == open('/Users/alexlintz/code/prediction-tracker/index.html').read())"` — must print `True`
- Check the HTML has `<!DOCTYPE html>` at start and `</html>` at end
- Check no "PROSE_PLACEHOLDER" markers remain
- Check no fetch-error text remains: `grep -iE "(403 this run|not accessible|null aggregations|no new.*this week)" archive/2026-04-20.html` should return nothing

Then commit and push.

### Report

At end, print a short summary of what changed, counts of sections removed / modified / added, and the commit SHA.
